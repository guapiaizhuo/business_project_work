server:
  port: 8889

spring:
  application:
    name: stock-service
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      config:
        import-check:
          enabled: false
      discovery:
        username: nacos
        password: nacos
        namespace: public